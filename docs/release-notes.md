# Release Notes - v1.0.0

- Initial Popstar DPC release scaffold.
- Device policy and VPN integration baseline.
- Three-tab Compose interface with purple-themed light/dark support.
- Password mode evaluator and secure storage primitives.
- CI workflow for debug/release assembly and unit tests.

- Removed committed binary debug keystore; build now uses SDK-generated debug keystore.
